export enum Chef {
  DIALER_CONTRACT,
  MINICHEF,
  OLD_FARMS,
}

export enum Rewarder {
  SIMPLE,
  COMPLEX,
  ALCX,
}

export enum PairType {
  SWAP,
  KASHI,
}

export enum OnsenModalView {
  Liquidity,
  Staking,
  Position,
  AddLiquidityConfirm,
}
